import { View, Text } from "react-native";
import React from "react";
import ViewProfileScreen from "@/features/profile/ViewProfileScreen";

const view = () => {
  return <ViewProfileScreen />;
};

export default view;
