import { View, Text } from "react-native";
import React from "react";
import UpdateApp from "@/features/screens/UpdateApp";

const UpdateAppModal = () => {
  return <UpdateApp />;
};

export default UpdateAppModal;
