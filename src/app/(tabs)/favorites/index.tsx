import { View, Text } from 'react-native'
import React from 'react'
import FavoritesList from '@/features/listings/favorites/FavoritesList'

const index = () => {
  return (
    <FavoritesList />
  )
}

export default index